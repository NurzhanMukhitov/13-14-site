# 2026-04-07

## Цель дня

Доработать layout сайта: переместить сферу в раздел Profile, настроить Profile-страницу, убрать лишние элементы на Title.

## Что делали

1. **Перенос текстового блока на Profile** — блок `PROJECT 001–004` + `Untitled (Profile)` + `info@13-14.space` выровнен в правую верхнюю четверть экрана (`left: 52%, top: 12%`).

2. **Перенос сферы в раздел Profile** — сфера убрана из PROJECT 001 и добавлена в левую часть PROFILE-секции (`left: 8px, top: 12%, h: 76%, w: 44%`).

3. **PROJECT 001 → заглушка** — убран `withSphere: true`, добавлен subtitle `"Generative Particle Surface"`, экран теперь показывает стандартный placeholder как PROJECT 002–004.

4. **Прозрачный фон сферы** — в `sphere-canvas.tsx` заменён `s.background(0)` на `s.clear()`, добавлен `canvas.style.background = "transparent"`. Серая подложка осталась видна (нужен `{ alpha: true }` в `createCanvas` — оставлено на следующий шаг).

5. **Title-экран** — убраны все четыре угловые подписи (`PROJECT 000`, `UNTITLED (TITLE)`, `PROJECT 005`, `PROJECT 001`). Остался только `13 | 14` по центру.

## Текущее рабочее состояние (боевое)

- `app/page.tsx` — горизонтальный скролл, 5 экранов + Profile.
- `components/sphere-canvas.tsx` — p5-сфера (подключена в Profile).
- `components/sphere-r3f.tsx` — WebGL-эксперимент, не подключён.

## Что оставили на следующий шаг

1. Убрать серую подложку сферы окончательно — добавить `{ alpha: true }` в `s.createCanvas(...)` в `sphere-canvas.tsx`.
2. Проработать содержимое экранов PROJECT 001–004 (реальные проекты вместо заглушек).
3. Рассмотреть возможность возврата к WebGL-сфере по плану из `2026-04-06.md`.
