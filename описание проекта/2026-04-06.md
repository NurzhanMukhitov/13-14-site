# 2026-04-06

## Контекст проекта

- Проект: `13-14-site` (Next.js, React, TypeScript).
- Задача: интерактивная сфера "13 | 14" в visual-секции.
- Эталон по поведению: старая p5-реализация (`sketch-sphere.js` из проекта `13.14`).

## Что делали

1. Пробовали переносить сферу в WebGL (`components/sphere-r3f.tsx`), включая:
   - частицы/цифры 1,3,4;
   - физику притяжения/отталкивания;
   - появление текста внутри сферы.
2. Много итераций по параметрам и логике.
3. Сравнили с эталоном p5 и зафиксировали, что WebGL пока визуально и по механике не совпадает с оригиналом.

## Текущее рабочее состояние (боевое)

- На странице подключен p5-вариант сферы:
  - `app/page.tsx` -> `SphereCanvas` из `components/sphere-canvas.tsx`.
- Это наиболее правильный и стабильный вариант относительно исходника.
- WebGL-компонент `components/sphere-r3f.tsx` оставлен как экспериментальный.

## Файлы, важные для сферы

- `app/page.tsx` — что именно рендерится на главной.
- `components/sphere-canvas.tsx` — p5-эталон (сейчас подключен).
- `components/sphere-r3f.tsx` — WebGL-эксперименты.
- Внешний референс:
  - `/Users/nmk_one/Downloads/github/13|14/13.14/sketch-sphere.js`
  - `/Users/nmk_one/Downloads/github/13|14/13.14/styles.css`
  - `/Users/nmk_one/Downloads/github/13|14/13.14/index.html`

## План на следующий шаг (если снова идем в WebGL)

1. Делать отдельный "чистый" порт, не правя текущий p5-боевой путь.
2. Ортографическая камера + координаты, эквивалентные p5-канвасу.
3. Прямой перенос `calculateCanvasSize` и всех чисел из p5.
4. Точный перенос физики без добавочных эффектов.
5. Подключать WebGL на страницу только после визуального совпадения с p5.
